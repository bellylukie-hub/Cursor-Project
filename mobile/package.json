{
  "name": "truckcontrol-mobile",
  "version": "2.5.16.0",
  "private": true,
  "description": "TruckControl mobile shell (Capacitor)",
  "scripts": {
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor/ios": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  },
  "capacitor": {
    "appId": "com.truckcontrol.app",
    "appName": "TruckControl",
    "webDir": ".."
  }
}
